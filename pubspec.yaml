name: flutter_chinese_keyboard
description: A flutter package for Chinese input virtual keyboard.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
